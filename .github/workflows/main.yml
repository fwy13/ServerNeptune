name: Deply to server.
on:
  push:
    branches: [main]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing
      run: ssh ${{ secrets.KEY }}@${{ secrets.HOST }}
